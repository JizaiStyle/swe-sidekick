{
  "version": 1,
  "objective": "Fix add(a, b) in src/add.py to return the sum instead of the difference.",
  "allowed_changes": ["src/add.py"],
  "acceptance_criteria": ["add(2, 3) returns 5", "Keep the existing function name and parameters"],
  "non_goals": ["Changing test contents", "Adding dependencies, calling external APIs or changing the design"],
  "verification": [["python3", "-m", "unittest", "discover", "-s", "tests", "-v"]]
}
