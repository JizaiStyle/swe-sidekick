interface:
  display_name: "SWE-2 Sidekick"
  short_description: "現在のリードが設計・レビューし、SWE-2に限定した実装を委譲する"
  default_prompt: "Use $swe-sidekick for a bounded implementation task; keep design and final review with the current lead."
policy:
  allow_implicit_invocation: false
